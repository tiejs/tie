{
  "name": "@tiejs/schedule",
  "version": "1.9.0",
  "description": "Schedule plugin TieJS",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsnl dev",
    "build": "tsnl build",
    "test": "tsnl test",
    "lint": "tsnl lint",
    "lint:fix": "tsnt lint --fix"
  },
  "files": [
    "**/*.js",
    "**/*.d.ts"
  ],
  "dependencies": {
    "@tiejs/common": "^1.9.0",
    "node-cron": "^3.0.0"
  },
  "devDependencies": {
    "@types/node-cron": "^2.0.3"
  }
}